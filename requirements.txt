# Core runtime dependencies for eventbt.
# pandas is pinned >=2.2 for the "ME" (month-end) resample alias used in metrics.
# matplotlib (plotting) and yfinance (optional data loader) are extras -- see
# pyproject.toml [project.optional-dependencies]. Install them with:
#   pip install -e .[plot,data]
numpy>=1.24
pandas>=2.2
